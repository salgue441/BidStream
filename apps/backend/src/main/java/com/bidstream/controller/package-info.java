/**
 * Package containing controller classes that handle HTTP requests.
 * These classes define API endpoints and are reponsible for request
 * processing, input validation, and response generation. Controllers
 * are typically annotated with {@code @RestController} or {@code @Controller}.
 */

package com.bidstream.controller;
