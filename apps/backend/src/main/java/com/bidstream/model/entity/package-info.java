/**
 * Package containing entity classes that represent the domain model.
 * These classes map to database tables and define the core data structure
 * of the application. Each entity typically corresponds to a database table.
 */

package com.bidstream.model.entity;
